import 'package:flutter/material.dart';

class SettingseSecreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Settings Secreen',
        style: TextStyle(fontSize: 30),
      ),
    );
  }
}
